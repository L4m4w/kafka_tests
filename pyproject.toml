[project]
name = "kafka-tests"
version = "0.1.0"
description = ""
authors = [
    {name = "Evgen Larkin",email = "l4p41@yandex.com"}
]
requires-python = ">=3.12"
dependencies = [
    "httpx (>=0.28.1,<0.29.0)",
    "pytest (>=9.1.1,<10.0.0)",
    "kafka-python (>=3.0.6,<4.0.0)",
    "pika (>=1.4.1,<2.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
